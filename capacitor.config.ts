import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ourspace.firechatApp',
  appName: 'firestore-chat-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
